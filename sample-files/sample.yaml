# Sample YAML configuration
service:
  name: inspector-demo
  port: 8080
  replicas: 2

logging:
  level: info
  format: json

features:
  enabled:
    - previews
    - search
  flags:
    darkMode: true
    beta: false

paths:
  data: ./data
  tmp: /tmp/demo
