# Host inventory — 2026-07-31

This is a read-only, secret-free snapshot captured before Fitz implementation began. Existing runtime services were not modified.

## Platform

- Windows 11 host with Ubuntu 24.04.3 LTS under WSL2.
- GPU: NVIDIA GeForce RTX 5090, 32,607 MiB reported VRAM.
- Windows Node.js: 25.6.0.
- WSL Node.js: 22.23.2.
- Fitz pins Node.js 22.23.2 initially to match the installed Pi runtime.

## Current NInfer service

- Public local proxy: `ninfer-on-demand.service`.
- Proxy executable: `/usr/local/sbin/ninfer-on-demand-proxy`.
- Stable loopback endpoint: `127.0.0.1:18080`.
- Internal backend: `ninfer.service` on `127.0.0.1:18081`.
- Idle eviction: 60 seconds.
- Current selected profile at capture time: `27b`.
- Current context target: 100,000 tokens.
- General installation: `/opt/ninfer`.
- General installation Git revision: `f4f9d39f9851572ca592dbe18b76fcdb953a35bb`.
- Server binary: `/opt/ninfer/build/apps/ninfer-serve`.

### Current recipes

| Profile | Model ID | Artifact | Speculative flags |
| --- | --- | --- | --- |
| `27b` | `qwen3.6-27b` | `/opt/ninfer/models/qwen3_6_27b_nvfp4.ninfer` | `--spec mtp --draft-tokens 3 --lm-head-draft` |
| `35b` | `qwen3.6-35b-a3b` | `/opt/ninfer/models/qwen3_6_35b_a3b.ninfer` | `--spec mtp --draft-tokens 4 --lm-head-draft` |

Both current launch profiles bind to loopback and use `--max-context 100000`, `--kv-dtype int8`, and `--no-thinking`.

Model-card checksums:

- 27B: `bce5f00d066c0f20f1317bf1fdcb458264cf95837c3b1f3fbec163694627893a`
- 35B: `1fb9ea0b5b8561e49d9604115ec89e5d9f2b6f6434e32c37c57fffd480a325d2`

## Protected legacy installation

`/root/ninfer` is an older, dirty working tree at revision `fc9a7d00f660`. It contains untracked runtime data and must not be modified, cleaned, rebuilt, or used as the Fitz-managed installation.

The historical validated checkout `/root/ninfer-latest-8aa4988` remains at revision `8aa49883deabfee4a660801455a1be5483155e5a`.

## Pi

- Package: `@earendil-works/pi-coding-agent`.
- Version: `0.83.0`.
- License declared by package: MIT.
- Runtime requirement: Node.js `>=22.19.0`.
- Installation root: `/root/.local/share/pi-node/node-v22.23.2-linux-x64/lib/node_modules`.
- SDK exposes `createAgentSession`, event subscriptions, in-memory or persistent session managers, manual compaction, and compaction lifecycle events.
- `session_before_compact` can cancel default compaction or provide a Fitz-owned summary, so the initial integration spike does not appear to require a fork.

## Tailscale

No Tailscale CLI, daemon, or service was found on either the Windows host path or inside Ubuntu WSL. Remote-access work therefore requires a separate installation and onboarding decision during Milestone 5.
