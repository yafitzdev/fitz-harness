name: Eternal Minimal (experimental)
description: The visible catalog stays exactly the Minimal pair (bash + str_replace_editor) forever; the full Standard toolset runs for real behind the dshx bash gateway. No anchor, no promotion, no catalog growth.
order: 9
